<div class="folder">
	<div class="icon-container">
		📁
	</div>
	<p class="name"><%= folder.name %></p>
	<%  if (folder.files.length !== 0 ) { %>
	<p class="contentCount"><%=folder.files.length %> files</p>
	<% }%>
</div>